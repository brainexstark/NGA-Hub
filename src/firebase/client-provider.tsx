'use client';

/**
 * Firebase client provider removed — re-exports Supabase DB provider.
 * Kept as a shim for backward-compatible imports.
 */

export { DbProvider as FirebaseClientProvider } from '../lib/db-provider';
